import 'package:flutter/material.dart';

/// Paleta oficial do Musical Map
/// Usado para manter consistência visual em toda a aplicação
class AppColors {
  AppColors._();

  // Backgrounds
  static const Color backgroundPrimary = Color(0xFF111315);
  static const Color surface = Color(0xFF1B1F24);
  static const Color surfaceSecondary = Color(0xFF2A2F36);

  // Texto
  static const Color textPrimary = Color(0xFFF3F1EA);
  static const Color textSecondary = Color(0xFFB8BDC7);

  // Bordas
  static const Color border = Color(0xFF313742);

  // Destaques e CTAs
  static const Color primary = Color(0xFFC7FF4A);
  static const Color secondary = Color(0xFF7C5CFF);

  // Feedback
  static const Color success = Color(0xFF42D392);
  static const Color warning = Color(0xFFFFB84D);
  static const Color error = Color(0xFFFF6B6B);
}
